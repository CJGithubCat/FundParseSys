<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zsh.labouCapital.dao.IndexCountMapper">
	<select id="queryJyzSalaryNoticeCount" parameterType="map" resultType="int">
		SELECT 
		  COUNT(*) 
		FROM
		  dbo.t_station_exam_notice_result t 
		  INNER JOIN t_company_path tcp ON t.company_id = tcp.company_id 
		WHERE tcp.company_path LIKE (CONVERT(VARCHAR(100),#{companyPath})+'%')		
		  AND t.salary_status = #{salaryStatus} ; 
	</select>
	
	<select id="queryXgsSalaryNoticeCount" parameterType="map" resultType="int">
		SELECT 
		  COUNT(1) 
		FROM
		  dbo.t_department_exam_notice_result t 
		  INNER JOIN t_company_path tcp 
		    ON t.company_id = tcp.company_id 
		WHERE tcp.company_path LIKE (CONVERT(VARCHAR(100),#{companyPath})+'%')
		  and t.salary_status = #{salaryStatus} ; 
	</select>
	
	<select id="queryJyzExamNoticeCount" parameterType="map" resultType="int">
		SELECT 
		  COUNT(*) 
		FROM
		  dbo.t_station_exam_notice_result t 
		  INNER JOIN t_company_path tcp ON t.company_id = tcp.company_id 
		WHERE tcp.company_path LIKE (CONVERT(VARCHAR(100),#{companyPath})+'%')		
		  AND t.status = #{status} ; 
	</select>
	
	<select id="queryXgsExamNoticeCount" parameterType="map" resultType="int">
		SELECT 
		  COUNT(1) 
		FROM
		  dbo.t_department_exam_notice_result t 
		  INNER JOIN t_company_path tcp 
		    ON t.company_id = tcp.company_id 
		WHERE tcp.company_path LIKE (CONVERT(VARCHAR(100),#{companyPath})+'%')
		  and t.status = #{status} ;
	</select>
	
</mapper>